import BookingPageLoading from "@/components/booking/BookingPageLoading";

/** Route-level loading while the book segment resolves (matches dynamic import placeholder). */
export default function BookQuoteLoading() {
  return <BookingPageLoading />;
}
